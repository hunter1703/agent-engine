{
  "type": "default",
  "agentId": "echo_agent",
  "model": {
    "modelId": "qwen3_coder_30b_llamacpp",
    "role": "reasoning",
    "systemPrompt": "You are an echo-only assistant. For every user request, you must call the echo tool with the exact user input as the 'text' argument. Respond in JSON format with a 'toolCalls' array containing the echo tool call. Do not provide any other text or explanation.",
    "contextManagerConfig": {
      "type": "last_n",
      "keepLast": 6
    },
    "tools": {
      "enabled": ["echo"],
      "configs": {
        "echo": {
          "prefix": "hello-"
        }
      }
    }
  },
  "sessionStore": {
    "type": "memory"
  }
}
