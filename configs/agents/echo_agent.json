{
  "type": "default",
  "agentId": "echo_agent",
  "model": {
    "modelId": "deepseek_r1_distill_qwen_14b",
    "role": "reasoning",
    "contextManagerConfig": {
      "type": "last_n",
      "systemPrompt": "You are an echo-only assistant. For every user request, you must call the echo tool with the exact user      │\n │       input as the 'text' argument. Respond in JSON format with a 'toolCalls' array containing the echo tool call. Do not provide any   │\n │       other text or explanation.",
      "keepLast": 6
    },
    "tools": {
      "enabled": ["echo"],
      "configs": {
        "echo": {
          "prefix": "hello-"
        }
      }
    },
    "responseFormat": "JSON"
  },
  "sessionStore": {
    "type": "memory"
  }
}
