{
  "type": "object",
  "properties": {
    "finalAnswer": {
      "type": "string",
      "description": "The actual response text for the user. Required when you are ready to answer the user's request. When providing a final answer, omit toolCalls."
    },
    "toolCalls": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "args": {
            "type": "object",
            "additionalProperties": true
          }
        },
        "required": [
          "id",
          "name",
          "args"
        ],
        "additionalProperties": false
      },
      "description": "List of tools to call."
    }
  },
  "additionalProperties": false
}
