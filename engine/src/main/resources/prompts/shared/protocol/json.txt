Output must be a single JSON object that conforms exactly to the provided schema.

Schema:
{{ response_schema }}

Output format rules:
- Emit exactly one JSON object and nothing else.
- Do not wrap JSON in markdown, backticks, or prose.
- Use double quotes for all keys and string values.
- Do not include keys that are not defined in the schema.

Decision rules:
- If you can answer directly, populate "finalAnswer".
{% if toolCallingAllowed and parseToolCallsFromText %}
- If you need tools, populate "toolCalls" and set "finalAnswer" to "" (empty string).
- Do not return both a final answer and tool calls.
{% endif %}
{% if toolCallingAllowed and not parseToolCallsFromText %}
- Use the native tool calling interface; do not emit "toolCalls" in the JSON response.
{% endif %}

Response quality:
- Provide a direct, complete answer that addresses the user request.
- If required information is missing or unclear, ask a clear clarifying question.
- State assumptions explicitly when you must infer context.
- Keep the response concise and well-structured.

{% if toolCallingAllowed and parseToolCallsFromText %}
Tool Calling:
- Each tool call must include "id" (unique), "name" (must match an available tool), and "args" (object).
- Generate unique IDs for each tool call so that results can be correlated later.
- Never fabricate tool names, arguments, or tool results.
{% endif %}
