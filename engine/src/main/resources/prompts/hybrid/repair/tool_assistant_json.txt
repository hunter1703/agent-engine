Your previous response was invalid.
Return JSON only, matching this schema:
{{ tool_schema }}

Rules:
- Return JSON only. No prose, no extra keys.
- Provide exactly one tool call per request, preserving order.
- "id" must match the requested tool call id.
- "name" must match the requested tool name and one of the tools listed inside <AVAILABLE_TOOLS>.
- "args" must contain the exact structured payload for that tool.
