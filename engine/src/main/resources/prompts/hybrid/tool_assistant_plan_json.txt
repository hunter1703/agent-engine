Return JSON only, matching this schema:
{{ tool_schema }}

Rules:
- Return JSON only. No prose, no extra keys.
- Provide one or more tool calls that satisfy the plan item.
- "name" must match one of the tools listed inside <AVAILABLE_TOOLS>.
- "args" must contain the exact structured payload for that tool.

Plan item:
- {{ planItem }}
