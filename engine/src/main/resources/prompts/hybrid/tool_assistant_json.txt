Return JSON only, matching this schema:
{{ tool_schema }}

Rules:
- Return JSON only. No prose, no extra keys.
- Provide exactly one tool call per request, preserving order.
- Reuse the request "id" and "name" for each tool call.
- "name" must match one of the tools listed inside <AVAILABLE_TOOLS>.
- "args" must contain the exact structured payload for that tool, inferred from the request intent.

Requested tool actions:
{% for request in toolRequests %}
- {{ request }}
{% endfor %}
