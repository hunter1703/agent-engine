plugins {
    id 'com.agentengine.java-application-conventions'
    alias(libs.plugins.quarkus)
}

configurations.all {
    exclude group: 'org.springframework.boot', module: 'spring-boot-starter-logging'
    exclude group: 'ch.qos.logback', module: 'logback-classic'
    exclude group: 'ch.qos.logback', module: 'logback-core'
}

dependencies {
    implementation(enforcedPlatform(libs.quarkus.bom))
    implementation libs.slf4j.api
    implementation project(':engine')
    implementation project(':interfaces:common')
    implementation libs.quarkus.logging.json
    implementation libs.quarkus.rest.jackson
    implementation libs.quarkus.smallrye.openapi
    implementation libs.quarkus.mongodb.client

    // AG-UI Core components
    implementation libs.ag.ui.core

    // AG-UI Server components
    implementation libs.ag.ui.server

    // AG-UI JSON utilities
    implementation libs.ag.ui.json

    // AG-UI HTTP client (if you need to connect to remote agents)
    implementation libs.ag.ui.http
    implementation libs.ag.ui.ok.http

    testImplementation libs.quarkus.junit5
    testImplementation libs.assertj.core
    testImplementation libs.mockito.core
}

// Configuration for faster Quarkus Dev mode
def isCiServer = System.getenv("CI_SERVER") != null
def isCi = System.getenv("CI") != null
def isContinuousIntegration = isCiServer || isCi

// Disable hot replacement in CI environments to speed up builds
if (isContinuousIntegration) {
    tasks.withType(JavaCompile) {
        options.compilerArgs += ['-Xlint:all', '-Werror']
    }
}

// Configure quarkusDev task for faster startup
tasks.named('quarkusDev') {
    // Enable hot replacement by default
    // Note: Quarkus Dev task uses system properties differently
    // Properties are typically set via quarkusBuild or through gradle.properties
}

tasks.withType(Test).configureEach {
    systemProperty 'java.util.logging.manager', 'org.jboss.logmanager.LogManager'
}
