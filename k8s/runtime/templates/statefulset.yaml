apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "agent-engine-runtime.fullname" . }}
  namespace: {{ include "agent-engine-runtime.namespace" . }}
  labels:
    {{- include "agent-engine-runtime.labels" . | nindent 4 }}
spec:
  serviceName: {{ .Values.service.headlessName }}
  replicas: {{ .Values.replicas }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  podManagementPolicy: {{ .Values.podManagementPolicy }}
  updateStrategy:
    {{- toYaml .Values.updateStrategy | nindent 4 }}
  selector:
    matchLabels:
      {{- include "agent-engine-runtime.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "agent-engine-runtime.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- if not .Values.config.existingConfigMap }}
        checksum/application-properties: {{ .Values.config.applicationProperties | toYaml | sha256sum }}
        {{- end }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "agent-engine-runtime.serviceAccountName" . }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automountToken }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      {{- if .Values.priorityClassName }}
      priorityClassName: {{ .Values.priorityClassName }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: runtime
          image: {{ include "agent-engine-runtime.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          env:
            - name: QUARKUS_HTTP_PORT
              value: {{ .Values.service.httpPort | quote }}
            - name: QUARKUS_GRPC_SERVER_PORT
              value: {{ .Values.service.grpcPort | quote }}
            {{- if .Values.bootstrap.mongodb.existingSecret }}
            - name: MONGODB_CONNECTION_STRING
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.bootstrap.mongodb.existingSecret }}
                  key: {{ .Values.bootstrap.mongodb.connectionStringKey }}
            {{- end }}
            - name: QUARKUS_CONFIG_LOCATIONS
              value: file:/config/application.properties
            {{- if .Values.java.opts }}
            - name: JAVA_OPTS
              value: {{ .Values.java.opts | quote }}
            {{- end }}
            {{- if .Values.java.args }}
            - name: JAVA_ARGS
              value: {{ .Values.java.args | quote }}
            {{- end }}
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: PEKKO_HOSTNAME
              value: "$(POD_NAME).{{ .Values.service.headlessName }}.$(POD_NAMESPACE).svc.cluster.local"
            {{- range $name, $value := .Values.extraEnv }}
            - name: {{ $name }}
              value: {{ $value | quote }}
            {{- end }}
          {{- if or .Values.extraEnvFromSecrets .Values.extraEnvFromConfigMaps }}
          envFrom:
            {{- range .Values.extraEnvFromSecrets }}
            - secretRef:
                name: {{ . }}
            {{- end }}
            {{- range .Values.extraEnvFromConfigMaps }}
            - configMapRef:
                name: {{ . }}
            {{- end }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.service.httpPort }}
            - name: grpc
              containerPort: {{ .Values.service.grpcPort }}
            - name: pekko
              containerPort: {{ .Values.service.pekkoPort }}
          readinessProbe:
            tcpSocket:
              port: grpc
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds }}
          livenessProbe:
            tcpSocket:
              port: grpc
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
          {{- if .Values.probes.startup.enabled }}
          startupProbe:
            tcpSocket:
              port: grpc
            periodSeconds: {{ .Values.probes.startup.periodSeconds }}
            failureThreshold: {{ .Values.probes.startup.failureThreshold }}
            timeoutSeconds: {{ .Values.probes.startup.timeoutSeconds }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: application-properties
              mountPath: /config/application.properties
              subPath: application.properties
              readOnly: true
            {{- if .Values.tmpVolume.enabled }}
            - name: tmp
              mountPath: /tmp
            {{- end }}
      volumes:
        - name: application-properties
          configMap:
            name: {{ include "agent-engine-runtime.applicationPropertiesConfigMapName" . }}
        {{- if .Values.tmpVolume.enabled }}
        - name: tmp
          emptyDir:
            {{- if .Values.tmpVolume.sizeLimit }}
            sizeLimit: {{ .Values.tmpVolume.sizeLimit }}
            {{- end }}
        {{- end }}
