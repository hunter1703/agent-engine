apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-engine-core.fullname" . }}
  namespace: {{ include "agent-engine-core.namespace" . }}
  labels:
    {{- include "agent-engine-core.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "agent-engine-core.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.httpPort }}
      targetPort: http
    - name: grpc
      port: {{ .Values.service.grpcPort }}
      targetPort: grpc
