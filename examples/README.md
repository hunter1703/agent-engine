# Examples

This folder contains sample plugin configs and usage patterns that align with the
current Java engine.

## Layout

- `examples/plugins/` → plugin config examples

## Plugin Config Example

- `examples/plugins/echo-agent/config.json` shows a minimal plugin config
  that enables a tool and points at model configs.
