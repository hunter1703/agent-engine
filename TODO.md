# TODO

- Emit `tasks.md`, `walkthrough.md`, and `implementation.md` artifacts from the plan/tasks/walkthrough agents.
- remove trivial tests
- add integration tests
- Add static imports
- remove needless comments
- code hygiene, public static methods first, public methods second, private methods third, private static methods fourth
- plans
  - why `plan_id` and `id` in plans?
  - should set random plan id if not provided
  - should use static methods if possible
  - variable names should be more descriptive