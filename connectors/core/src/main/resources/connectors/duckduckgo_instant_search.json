{
  "id": "duckduckgo-instant-answer",
  "appName": "duckduckgo",
  "endpoint": {
    "method": "GET",
    "baseUrl": "https://api.duckduckgo.com",
    "path": "/"
  },
  "query": {
    "q": "${input.query}",
    "format": "json"
  },
  "responseMapping": {
    "output": "${ def data = response; if (data == null) return null; def result = [:]; result.abstract = data.AbstractText; result.url = data.AbstractURL; result.source = data.AbstractSource; result.heading = data.Heading; if (data.RelatedTopics != null) result.relatedTopics = data.RelatedTopics; return result }"
  }
}
