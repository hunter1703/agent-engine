plugins {
    id 'com.agentengine.java-library-conventions'
}

dependencies {
    // Annotation-only APIs: compile-time markers, runtime provided by Quarkus
    compileOnly libs.jakarta.inject.api
    compileOnly libs.jakarta.enterprise.cdi.api
    compileOnly libs.jakarta.validation.api
    
    // Core Dependencies
    implementation project(':util:common')
    
    // JGroups
    implementation libs.jgroups
    implementation libs.jgroups.kubernetes
    
    // Guava (exposed in DistributedCache constructor)
    api libs.guava
    
    // Logging
    implementation libs.slf4j.api
}
