# Headless service required by the Postgres StatefulSet for pod DNS identity.
# Application pods connect to Postgres via the regular ClusterIP service (postgres).
apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-engine.infra-base.instance" . }}-headless
  namespace: {{ include "agent-engine.infra-base.namespace" . }}
  labels:
    {{- include "agent-engine.infra-base.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    {{- include "agent-engine.infra-base.selectorLabels" . | nindent 4 }}
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
