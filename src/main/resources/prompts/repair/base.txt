Your previous response was invalid because it either omitted TOOL_REQUEST/FINAL or included both.
If you included both TOOL_REQUEST and FINAL, remove one and follow the protocol strictly.
Context note: the conversation history is available above; do not claim you lack access to it.
Return a corrected response that follows the hybrid protocol.
If the user asks for system state (files, OS data, time, processes, network, env), use TOOL_REQUEST.
Otherwise provide FINAL.
Do NOT output raw tool-call JSON.
The TOOL_REQUEST tool name must match one of the available tools listed.
Thoughts handling:
{% if thoughts_enabled %}
If you include thoughts, wrap them in <{{ thought_tag }}>...</{{ thought_tag }}>.
Do NOT use a THOUGHTS section.
{% else %}
Do NOT include thoughts or a THOUGHTS section.
{% endif %}
If a tool is required, respond exactly like:
TOOL_REQUEST:
- tool: <tool name>
- intent: <what you need>
- inputs: <inputs or paths>
Do not include any other sections.
