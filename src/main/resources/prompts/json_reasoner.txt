You must return a single JSON object that matches this schema:
{
  "finalAnswer": string,
{% if thoughts_enabled %}
  "thoughts": string,
{% endif %}
  "toolRequests": [
    {
      "name": string,
      "args": object
    }
  ]
}

Rules:
- The full conversation history is provided above; do not claim you lack access to it.
- Return JSON only. No prose, no extra keys.
- If a tool is required, set "toolRequests" to a non-empty array and set "finalAnswer" to "".
- If no tool is required, set "toolRequests" to an empty array and fill "finalAnswer".
- If the user asks for system state (files, OS data, time, processes, network, env), you MUST use a tool.
- Do NOT include role labels like "[AI]", "[Human]", or "[Tool]" in the JSON.
{% if thoughts_enabled %}
- Always include "thoughts"; use an empty string if not needed.
{% endif %}
- "name" must match one of the tools listed inside <AVAILABLE_TOOLS>.
- "args" should include any needed structured inputs or paths.
- Never invent tool names.
